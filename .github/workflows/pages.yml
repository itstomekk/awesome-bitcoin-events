name: Deploy Signal Atlas to GitHub Pages

# Builds the Astro site and publishes it to https://itstomekk.github.io/awesome-bitcoin-events/
#   - pull_request: build + list check only (catches broken event files), no deploy
#   - push to main / manual run: build and deploy
# AUDIT NOTES: the Python test suite is not run here, and there is no scheduled run, so
# the site's "upcoming" view only refreshes when something is pushed to main.

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    permissions:
      contents: read
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
      - name: Install dependencies
        run: npm ci
      # Fails if README.md / EVENTS.md were not regenerated after an event file changed,
      # or if someone edited the generated README table by hand.
      - name: Check generated event lists
        run: npm run check:event-lists
      - name: Build Astro site
        run: npm run build
        env:
          PUBLIC_BASE_PATH: /awesome-bitcoin-events
      - name: Upload Pages artifact
        if: github.event_name != 'pull_request'
        uses: actions/upload-pages-artifact@v3
        with:
          path: ./dist

  deploy:
    needs: build
    if: (github.event_name == 'push' && github.ref == 'refs/heads/main') || github.event_name == 'workflow_dispatch'
    permissions:
      contents: read
      pages: write
      id-token: write
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
